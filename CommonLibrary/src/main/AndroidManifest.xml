<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.library.common">

    <uses-permission android:name="android.permission.INTERNET" />  <!-- 允许程序访问有关网络权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /><!--判断网络是否可用-->

    <application>
        <activity android:name=".base.BaseActivity" />
        <activity android:name="com.spark.mvvmjava.base.mvvm.MVVMBaseActivity" />
    </application>
</manifest>
